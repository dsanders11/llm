// Generated by dynwinrt-codegen — do not edit
import { IIterable_EmbeddingVector } from './IIterable_EmbeddingVector.js';
import { AIFeatureReadyResult } from './AIFeatureReadyResult.js';
import { AIFeatureReadyState } from './AIFeatureReadyState.js';
import { ContentFilterOptions } from './ContentFilterOptions.js';
import { EmbeddingVector } from './EmbeddingVector.js';
import { LanguageModelContext } from './LanguageModelContext.js';
import { LanguageModelEmbeddingVectorResult } from './LanguageModelEmbeddingVectorResult.js';
import { LanguageModelOptions } from './LanguageModelOptions.js';
import { LanguageModelResponseResult } from './LanguageModelResponseResult.js';
import { IClosable } from './IClosable.js';

/**
 * A WinRT async operation that supports progress reporting and cancellation.
 *
 * - `await op` — wait for the final result
 * - `op.progress(cb)` — receive incremental progress updates (chainable)
 * - `op.cancel()` — cancel the underlying WinRT operation
 */
type WinRTAsyncWithProgress<T, P = unknown> = Promise<T> & {
  /** Register a callback for progress updates. Returns `this` for chaining. */
  progress(cb: (value: P) => void): WinRTAsyncWithProgress<T, P>;
  /** Get the underlying Promise (equivalent to awaiting directly). */
  toPromise(): Promise<T>;
  /** Cancel the underlying WinRT async operation. */
  cancel(): void;
};

/**
 * Represents a local small language model.
 */
export declare class LanguageModel {
  private constructor();
  /**
   * Returns the readiness state of the language model.
   * @returns The readiness state of the language model.
   */
  static getReadyState(): AIFeatureReadyState;
  /**
   * Ensures the underlying language model is ready for use by downloading and installing any required components.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns An asynchronous action with progress that returns an AIFeatureReadyResult on completion.
   */
  static ensureReadyAsync(
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<AIFeatureReadyResult, number>;
  /**
   * Asynchronously creates a new instance of the language model object.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns A language model object.
   */
  static createAsync(signal?: AbortSignal): Promise<LanguageModel>;
  /** Release underlying WinRT resources (IClosable). */
  close(): void;
  /** Cast this object to another WinRT interface. */
  as<T>(InterfaceClass: { from(obj: any): T }): T;
  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync(
    prompt: string,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;
  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param options Options that affect how the language model responds to the prompt.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync(
    prompt: string,
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;
  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param context The context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response. The context window acts like the model's short-term memory, holding the input prompt and the preceding conversation to maintain coherence.
   * @param prompt The string prompt requiring a response.
   * @param options Options that affect how the language model responds to the prompt.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync(
    context: LanguageModelContext,
    prompt: string,
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;
  generateResponseFromEmbeddingsAsync(
    promptEmbedding: IIterable_EmbeddingVector | EmbeddingVector[],
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;
  generateResponseFromEmbeddingsAsync(
    promptEmbedding: IIterable_EmbeddingVector | EmbeddingVector[],
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;
  generateResponseFromEmbeddingsAsync(
    context: LanguageModelContext,
    promptEmbedding: IIterable_EmbeddingVector | EmbeddingVector[],
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;
  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @returns The embedding vector representing the string prompt.
   */
  generateEmbeddingVectors(prompt: string): LanguageModelEmbeddingVectorResult;
  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @param contentFilterOptions Options that affect how the language model filters responses to the prompt.
   */
  generateEmbeddingVectors(
    prompt: string,
    contentFilterOptions: ContentFilterOptions,
  ): LanguageModelEmbeddingVectorResult;
  /**
   * Returns the index in the given prompt where the context window reached the maximum number of supported tokens.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength(prompt: string): bigint;
  /**
   * Returns the index in the given prompt where the given context window reached the maximum number of supported tokens.
   * @param context The context window.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the given context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength(context: LanguageModelContext, prompt: string): bigint;
  /**
   * Returns the unique identifier of an embedding vector object.
   * @returns The unique identifier of an embedding vector object.
   */
  getVectorSpaceId(): string;
  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @returns A context window
   */
  createContext(): LanguageModelContext;
  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @param systemPrompt The string prompt.
   * @returns A context window
   */
  createContext(systemPrompt: string): LanguageModelContext;
  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @param systemPrompt The string prompt.
   * @param contentFilterOptions Options that affect how the language model filters responses to the prompt.
   * @returns A context window
   */
  createContext(
    systemPrompt: string,
    contentFilterOptions: ContentFilterOptions,
  ): LanguageModelContext;
}

export declare class ILanguageModel2 {
  private constructor();

  /** @internal Cast a raw WinRT object to this interface. */
  static from(obj: any): ILanguageModel2;

  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync(
    prompt: string,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;

  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param options Options that affect how the language model responds to the prompt.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync2(
    prompt: string,
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;

  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param context The context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response. The context window acts like the model's short-term memory, holding the input prompt and the preceding conversation to maintain coherence.
   * @param prompt The string prompt requiring a response.
   * @param options Options that affect how the language model responds to the prompt.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync3(
    context: LanguageModelContext,
    prompt: string,
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;

  generateResponseFromEmbeddingsAsync(
    promptEmbedding: IIterable_EmbeddingVector | EmbeddingVector[],
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;

  generateResponseFromEmbeddingsAsync2(
    promptEmbedding: IIterable_EmbeddingVector | EmbeddingVector[],
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;

  generateResponseFromEmbeddingsAsync3(
    context: LanguageModelContext,
    promptEmbedding: IIterable_EmbeddingVector | EmbeddingVector[],
    options: LanguageModelOptions,
    signal?: AbortSignal,
  ): WinRTAsyncWithProgress<LanguageModelResponseResult, string>;

  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @returns The embedding vector representing the string prompt.
   */
  generateEmbeddingVectors(prompt: string): LanguageModelEmbeddingVectorResult;

  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @param contentFilterOptions Options that affect how the language model filters responses to the prompt.
   */
  generateEmbeddingVectors2(
    prompt: string,
    contentFilterOptions: ContentFilterOptions,
  ): LanguageModelEmbeddingVectorResult;

  /**
   * Returns the index in the given prompt where the context window reached the maximum number of supported tokens.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength(prompt: string): bigint;

  /**
   * Returns the index in the given prompt where the given context window reached the maximum number of supported tokens.
   * @param context The context window.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the given context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength2(context: LanguageModelContext, prompt: string): bigint;

  /**
   * Returns the unique identifier of an embedding vector object.
   * @returns The unique identifier of an embedding vector object.
   */
  getVectorSpaceId(): string;

  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @returns A context window
   */
  createContext(): LanguageModelContext;

  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @param systemPrompt The string prompt.
   * @returns A context window
   */
  createContext2(systemPrompt: string): LanguageModelContext;

  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @param systemPrompt The string prompt.
   * @param contentFilterOptions Options that affect how the language model filters responses to the prompt.
   * @returns A context window
   */
  createContext3(
    systemPrompt: string,
    contentFilterOptions: ContentFilterOptions,
  ): LanguageModelContext;
}
