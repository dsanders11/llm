// Generated by dynwinrt-codegen — do not edit
import {
  DynWinRtType,
  DynWinRtMethodSig,
  DynWinRtValue,
  DynWinRtArray,
  DynWinRtDelegate,
  WinGuid,
} from 'dynwinrt-js';
import { IIterable_EmbeddingVector } from './IIterable_EmbeddingVector.js';
import { AIFeatureReadyResult } from './AIFeatureReadyResult.js';
import { AIFeatureReadyState } from './AIFeatureReadyState.js';
import { ContentFilterOptions } from './ContentFilterOptions.js';
import { EmbeddingVector } from './EmbeddingVector.js';
import { LanguageModelContext } from './LanguageModelContext.js';
import { LanguageModelEmbeddingVectorResult } from './LanguageModelEmbeddingVectorResult.js';
import { LanguageModelOptions } from './LanguageModelOptions.js';
import { LanguageModelResponseResult } from './LanguageModelResponseResult.js';
import { IID_IClosable, IClosable } from './IClosable.js';
const _unwrap = (x) => x?._obj ?? x;

const IID_ILanguageModel = WinGuid.parse(
  '6331c629-8c86-5bfe-8c4e-9ca5573cc14b',
);
const IID_ILanguageModelStatics = WinGuid.parse(
  '8f18f9af-6095-553b-8d9d-6bcc98026546',
);
const IID_ILanguageModel2 = WinGuid.parse(
  '653b714e-f9b3-51cb-954f-5ea58f63ab89',
);

const _ILanguageModel = DynWinRtType.registerInterface(
  'ILanguageModel',
  IID_ILanguageModel,
);

const _ILanguageModelStatics = DynWinRtType.registerInterface(
  'ILanguageModelStatics',
  IID_ILanguageModelStatics,
)
  .addMethod(
    'GetReadyState',
    new DynWinRtMethodSig().addOut(
      DynWinRtType.enumType(
        'Microsoft.Windows.AI.AIFeatureReadyState',
        ['Ready', 'NotReady', 'NotSupportedOnCurrentSystem', 'DisabledByUser'],
        [0, 1, 2, 3],
      ),
    ),
  )
  .addMethod(
    'EnsureReadyAsync',
    new DynWinRtMethodSig().addOut(
      DynWinRtType.iAsyncOperationWithProgress(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.AIFeatureReadyResult',
          WinGuid.parse('936a78a6-c242-5937-9814-e512d4193a6d'),
        ),
        DynWinRtType.f64(),
      ),
    ),
  )
  .addMethod(
    'CreateAsync',
    new DynWinRtMethodSig().addOut(
      DynWinRtType.iAsyncOperation(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModel',
          WinGuid.parse('6331c629-8c86-5bfe-8c4e-9ca5573cc14b'),
        ),
      ),
    ),
  );

const _ILanguageModel2 = DynWinRtType.registerInterface(
  'ILanguageModel2',
  IID_ILanguageModel2,
)
  .addMethod(
    'GenerateResponseAsync',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addOut(
        DynWinRtType.iAsyncOperationWithProgress(
          DynWinRtType.runtimeClass(
            'Microsoft.Windows.AI.Text.LanguageModelResponseResult',
            WinGuid.parse('3a256fff-a426-5d3b-8e4b-3ac84162471e'),
          ),
          DynWinRtType.hstring(),
        ),
      ),
  )
  .addMethod(
    'GenerateResponseAsync2',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelOptions',
          WinGuid.parse('7f380003-5a09-5f1f-afb0-aa483e3670cc'),
        ),
      )
      .addOut(
        DynWinRtType.iAsyncOperationWithProgress(
          DynWinRtType.runtimeClass(
            'Microsoft.Windows.AI.Text.LanguageModelResponseResult',
            WinGuid.parse('3a256fff-a426-5d3b-8e4b-3ac84162471e'),
          ),
          DynWinRtType.hstring(),
        ),
      ),
  )
  .addMethod(
    'GenerateResponseAsync3',
    new DynWinRtMethodSig()
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelContext',
          WinGuid.parse('518b305c-7b69-5a33-8129-d47d6b8eec4e'),
        ),
      )
      .addIn(DynWinRtType.hstring())
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelOptions',
          WinGuid.parse('7f380003-5a09-5f1f-afb0-aa483e3670cc'),
        ),
      )
      .addOut(
        DynWinRtType.iAsyncOperationWithProgress(
          DynWinRtType.runtimeClass(
            'Microsoft.Windows.AI.Text.LanguageModelResponseResult',
            WinGuid.parse('3a256fff-a426-5d3b-8e4b-3ac84162471e'),
          ),
          DynWinRtType.hstring(),
        ),
      ),
  )
  .addMethod(
    'GenerateResponseFromEmbeddingsAsync',
    new DynWinRtMethodSig()
      .addIn(
        DynWinRtType.parameterized(
          WinGuid.parse('faa585ea-6214-4217-afda-7f46de5869b3'),
          [
            DynWinRtType.runtimeClass(
              'Microsoft.Windows.AI.Foundation.EmbeddingVector',
              WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
            ),
          ],
        ),
      )
      .addOut(
        DynWinRtType.iAsyncOperationWithProgress(
          DynWinRtType.runtimeClass(
            'Microsoft.Windows.AI.Text.LanguageModelResponseResult',
            WinGuid.parse('3a256fff-a426-5d3b-8e4b-3ac84162471e'),
          ),
          DynWinRtType.hstring(),
        ),
      ),
  )
  .addMethod(
    'GenerateResponseFromEmbeddingsAsync2',
    new DynWinRtMethodSig()
      .addIn(
        DynWinRtType.parameterized(
          WinGuid.parse('faa585ea-6214-4217-afda-7f46de5869b3'),
          [
            DynWinRtType.runtimeClass(
              'Microsoft.Windows.AI.Foundation.EmbeddingVector',
              WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
            ),
          ],
        ),
      )
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelOptions',
          WinGuid.parse('7f380003-5a09-5f1f-afb0-aa483e3670cc'),
        ),
      )
      .addOut(
        DynWinRtType.iAsyncOperationWithProgress(
          DynWinRtType.runtimeClass(
            'Microsoft.Windows.AI.Text.LanguageModelResponseResult',
            WinGuid.parse('3a256fff-a426-5d3b-8e4b-3ac84162471e'),
          ),
          DynWinRtType.hstring(),
        ),
      ),
  )
  .addMethod(
    'GenerateResponseFromEmbeddingsAsync3',
    new DynWinRtMethodSig()
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelContext',
          WinGuid.parse('518b305c-7b69-5a33-8129-d47d6b8eec4e'),
        ),
      )
      .addIn(
        DynWinRtType.parameterized(
          WinGuid.parse('faa585ea-6214-4217-afda-7f46de5869b3'),
          [
            DynWinRtType.runtimeClass(
              'Microsoft.Windows.AI.Foundation.EmbeddingVector',
              WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
            ),
          ],
        ),
      )
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelOptions',
          WinGuid.parse('7f380003-5a09-5f1f-afb0-aa483e3670cc'),
        ),
      )
      .addOut(
        DynWinRtType.iAsyncOperationWithProgress(
          DynWinRtType.runtimeClass(
            'Microsoft.Windows.AI.Text.LanguageModelResponseResult',
            WinGuid.parse('3a256fff-a426-5d3b-8e4b-3ac84162471e'),
          ),
          DynWinRtType.hstring(),
        ),
      ),
  )
  .addMethod(
    'GenerateEmbeddingVectors',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addOut(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelEmbeddingVectorResult',
          WinGuid.parse('4dbdb154-ee3c-56f6-a40b-413e95bd5acb'),
        ),
      ),
  )
  .addMethod(
    'GenerateEmbeddingVectors2',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.ContentSafety.ContentFilterOptions',
          WinGuid.parse('6808be9f-80d2-5136-8a8a-1f5c52c824ad'),
        ),
      )
      .addOut(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelEmbeddingVectorResult',
          WinGuid.parse('4dbdb154-ee3c-56f6-a40b-413e95bd5acb'),
        ),
      ),
  )
  .addMethod(
    'GetUsablePromptLength',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addOut(DynWinRtType.u64()),
  )
  .addMethod(
    'GetUsablePromptLength2',
    new DynWinRtMethodSig()
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelContext',
          WinGuid.parse('518b305c-7b69-5a33-8129-d47d6b8eec4e'),
        ),
      )
      .addIn(DynWinRtType.hstring())
      .addOut(DynWinRtType.u64()),
  )
  .addMethod(
    'GetVectorSpaceId',
    new DynWinRtMethodSig().addOut(DynWinRtType.guidType()),
  )
  .addMethod(
    'CreateContext',
    new DynWinRtMethodSig().addOut(
      DynWinRtType.runtimeClass(
        'Microsoft.Windows.AI.Text.LanguageModelContext',
        WinGuid.parse('518b305c-7b69-5a33-8129-d47d6b8eec4e'),
      ),
    ),
  )
  .addMethod(
    'CreateContext2',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addOut(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelContext',
          WinGuid.parse('518b305c-7b69-5a33-8129-d47d6b8eec4e'),
        ),
      ),
  )
  .addMethod(
    'CreateContext3',
    new DynWinRtMethodSig()
      .addIn(DynWinRtType.hstring())
      .addIn(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.ContentSafety.ContentFilterOptions',
          WinGuid.parse('6808be9f-80d2-5136-8a8a-1f5c52c824ad'),
        ),
      )
      .addOut(
        DynWinRtType.runtimeClass(
          'Microsoft.Windows.AI.Text.LanguageModelContext',
          WinGuid.parse('518b305c-7b69-5a33-8129-d47d6b8eec4e'),
        ),
      ),
  );

/**
 * Represents a local small language model.
 */
export class LanguageModel {
  _obj;
  static _s_ILanguageModelStatics;
  static s_ILanguageModelStatics() {
    return (LanguageModel._s_ILanguageModelStatics ??=
      DynWinRtValue.activationFactory(
        'Microsoft.Windows.AI.Text.LanguageModel',
      ).cast(IID_ILanguageModelStatics));
  }

  constructor(obj) {
    this._obj = obj.cast(IID_ILanguageModel);
  }
  /**
   * Returns the readiness state of the language model.
   * @returns The readiness state of the language model.
   */
  static getReadyState() {
    return _ILanguageModelStatics
      .method(6)
      .invoke(LanguageModel.s_ILanguageModelStatics(), [])
      .toNumber();
  }
  /**
   * Ensures the underlying language model is ready for use by downloading and installing any required components.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns An asynchronous action with progress that returns an AIFeatureReadyResult on completion.
   */
  static ensureReadyAsync(signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModelStatics
      .method(7)
      .invoke(LanguageModel.s_ILanguageModelStatics(), []);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new AIFeatureReadyResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toF64()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  /**
   * Asynchronously creates a new instance of the language model object.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns A language model object.
   */
  static async createAsync(signal) {
    if (signal?.aborted) throw signal.reason;
    const _op = _ILanguageModelStatics
      .method(8)
      .invoke(LanguageModel.s_ILanguageModelStatics(), []);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    try {
      const _v = await _op.toPromise();
      return new LanguageModel(_v);
    } catch (_e) {
      if (signal?.aborted) throw signal.reason;
      throw _e;
    } finally {
      if (_onAbort) signal.removeEventListener('abort', _onAbort);
    }
  }
  close() {
    IClosable.from(this._obj).close();
  }
  as(InterfaceClass) {
    return InterfaceClass.from(this._obj);
  }
  _generateResponseAsync_1(prompt, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(6)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        DynWinRtValue.hstring(prompt),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  _generateResponseAsync_2(prompt, options, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(7)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        DynWinRtValue.hstring(prompt),
        _unwrap(options),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  _generateResponseAsync_3(context, prompt, options, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(8)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        _unwrap(context),
        DynWinRtValue.hstring(prompt),
        _unwrap(options),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync(...args) {
    if (
      args.length >= 3 &&
      args[2] !== undefined &&
      !(args[2] instanceof AbortSignal)
    ) {
      return this._generateResponseAsync_3(args[0], args[1], args[2], args[3]);
    }
    if (
      args.length >= 2 &&
      args[1] !== undefined &&
      !(args[1] instanceof AbortSignal)
    ) {
      return this._generateResponseAsync_2(args[0], args[1], args[2]);
    }
    return this._generateResponseAsync_1(args[0], args[1]);
  }
  _generateResponseFromEmbeddingsAsync_1(promptEmbedding, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(9)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        Array.isArray(promptEmbedding)
          ? DynWinRtValue.createVector(
              promptEmbedding.map((_i) => _unwrap(_i)),
              DynWinRtType.runtimeClass(
                'Microsoft.Windows.AI.Foundation.EmbeddingVector',
                WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
              ),
            )
          : _unwrap(promptEmbedding),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  _generateResponseFromEmbeddingsAsync_2(promptEmbedding, options, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(10)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        Array.isArray(promptEmbedding)
          ? DynWinRtValue.createVector(
              promptEmbedding.map((_i) => _unwrap(_i)),
              DynWinRtType.runtimeClass(
                'Microsoft.Windows.AI.Foundation.EmbeddingVector',
                WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
              ),
            )
          : _unwrap(promptEmbedding),
        _unwrap(options),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  _generateResponseFromEmbeddingsAsync_3(
    context,
    promptEmbedding,
    options,
    signal,
  ) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(11)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        _unwrap(context),
        Array.isArray(promptEmbedding)
          ? DynWinRtValue.createVector(
              promptEmbedding.map((_i) => _unwrap(_i)),
              DynWinRtType.runtimeClass(
                'Microsoft.Windows.AI.Foundation.EmbeddingVector',
                WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
              ),
            )
          : _unwrap(promptEmbedding),
        _unwrap(options),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }
  generateResponseFromEmbeddingsAsync(...args) {
    if (
      args.length >= 3 &&
      args[2] !== undefined &&
      !(args[2] instanceof AbortSignal)
    ) {
      return this._generateResponseFromEmbeddingsAsync_3(
        args[0],
        args[1],
        args[2],
        args[3],
      );
    }
    if (
      args.length >= 2 &&
      args[1] !== undefined &&
      !(args[1] instanceof AbortSignal)
    ) {
      return this._generateResponseFromEmbeddingsAsync_2(
        args[0],
        args[1],
        args[2],
      );
    }
    return this._generateResponseFromEmbeddingsAsync_1(args[0], args[1]);
  }
  _generateEmbeddingVectors_1(prompt) {
    return new LanguageModelEmbeddingVectorResult(
      _ILanguageModel2
        .method(12)
        .invoke(this._obj.cast(IID_ILanguageModel2), [
          DynWinRtValue.hstring(prompt),
        ]),
    );
  }
  _generateEmbeddingVectors_2(prompt, contentFilterOptions) {
    return new LanguageModelEmbeddingVectorResult(
      _ILanguageModel2
        .method(13)
        .invoke(this._obj.cast(IID_ILanguageModel2), [
          DynWinRtValue.hstring(prompt),
          _unwrap(contentFilterOptions),
        ]),
    );
  }
  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @returns The embedding vector representing the string prompt.
   */
  generateEmbeddingVectors(...args) {
    if (
      args.length >= 2 &&
      args[1] !== undefined &&
      !(args[1] instanceof AbortSignal)
    ) {
      return this._generateEmbeddingVectors_2(args[0], args[1]);
    }
    return this._generateEmbeddingVectors_1(args[0]);
  }
  _getUsablePromptLength_1(prompt) {
    return _ILanguageModel2
      .method(14)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        DynWinRtValue.hstring(prompt),
      ])
      .toI64();
  }
  _getUsablePromptLength_2(context, prompt) {
    return _ILanguageModel2
      .method(15)
      .invoke(this._obj.cast(IID_ILanguageModel2), [
        _unwrap(context),
        DynWinRtValue.hstring(prompt),
      ])
      .toI64();
  }
  /**
   * Returns the index in the given prompt where the context window reached the maximum number of supported tokens.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength(...args) {
    if (
      args.length >= 2 &&
      args[1] !== undefined &&
      !(args[1] instanceof AbortSignal)
    ) {
      return this._getUsablePromptLength_2(args[0], args[1]);
    }
    return this._getUsablePromptLength_1(args[0]);
  }
  /**
   * Returns the unique identifier of an embedding vector object.
   * @returns The unique identifier of an embedding vector object.
   */
  getVectorSpaceId() {
    return _ILanguageModel2
      .method(16)
      .invoke(this._obj.cast(IID_ILanguageModel2), [])
      .toString();
  }
  _createContext_1() {
    return new LanguageModelContext(
      _ILanguageModel2
        .method(17)
        .invoke(this._obj.cast(IID_ILanguageModel2), []),
    );
  }
  _createContext_2(systemPrompt) {
    return new LanguageModelContext(
      _ILanguageModel2
        .method(18)
        .invoke(this._obj.cast(IID_ILanguageModel2), [
          DynWinRtValue.hstring(systemPrompt),
        ]),
    );
  }
  _createContext_3(systemPrompt, contentFilterOptions) {
    return new LanguageModelContext(
      _ILanguageModel2
        .method(19)
        .invoke(this._obj.cast(IID_ILanguageModel2), [
          DynWinRtValue.hstring(systemPrompt),
          _unwrap(contentFilterOptions),
        ]),
    );
  }
  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @returns A context window
   */
  createContext(...args) {
    if (
      args.length >= 2 &&
      args[1] !== undefined &&
      !(args[1] instanceof AbortSignal)
    ) {
      return this._createContext_3(args[0], args[1]);
    }
    if (
      args.length >= 1 &&
      args[0] !== undefined &&
      !(args[0] instanceof AbortSignal)
    ) {
      return this._createContext_2(args[0]);
    }
    return this._createContext_1();
  }
}

export class ILanguageModel2 {
  _obj;

  constructor(obj) {
    this._obj = obj;
  }

  static from(obj) {
    return new ILanguageModel2(obj.cast(IID_ILanguageModel2));
  }

  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync(prompt, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(6)
      .invoke(this._obj, [DynWinRtValue.hstring(prompt)]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }

  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param prompt The string prompt requiring a response.
   * @param options Options that affect how the language model responds to the prompt.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync2(prompt, options, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(7)
      .invoke(this._obj, [DynWinRtValue.hstring(prompt), _unwrap(options)]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }

  /**
   * Generates a response to a string prompt while returning progress for each token generated by the language model.
   * @param context The context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response. The context window acts like the model's short-term memory, holding the input prompt and the preceding conversation to maintain coherence.
   * @param prompt The string prompt requiring a response.
   * @param options Options that affect how the language model responds to the prompt.
   * @param signal Abort signal to cancel the underlying WinRT async operation.
   * @returns The response to the prompt.
   */
  generateResponseAsync3(context, prompt, options, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2
      .method(8)
      .invoke(this._obj, [
        _unwrap(context),
        DynWinRtValue.hstring(prompt),
        _unwrap(options),
      ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }

  generateResponseFromEmbeddingsAsync(promptEmbedding, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2.method(9).invoke(this._obj, [
      Array.isArray(promptEmbedding)
        ? DynWinRtValue.createVector(
            promptEmbedding.map((_i) => _unwrap(_i)),
            DynWinRtType.runtimeClass(
              'Microsoft.Windows.AI.Foundation.EmbeddingVector',
              WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
            ),
          )
        : _unwrap(promptEmbedding),
    ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }

  generateResponseFromEmbeddingsAsync2(promptEmbedding, options, signal) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2.method(10).invoke(this._obj, [
      Array.isArray(promptEmbedding)
        ? DynWinRtValue.createVector(
            promptEmbedding.map((_i) => _unwrap(_i)),
            DynWinRtType.runtimeClass(
              'Microsoft.Windows.AI.Foundation.EmbeddingVector',
              WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
            ),
          )
        : _unwrap(promptEmbedding),
      _unwrap(options),
    ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }

  generateResponseFromEmbeddingsAsync3(
    context,
    promptEmbedding,
    options,
    signal,
  ) {
    if (signal?.aborted) {
      const _rej = Promise.reject(signal.reason);
      _rej.catch(() => {});
      return Object.assign(_rej, {
        progress(_cb) {
          return this;
        },
        toPromise() {
          const _p = Promise.reject(signal.reason);
          _p.catch(() => {});
          return _p;
        },
        cancel() {},
      });
    }
    const _op = _ILanguageModel2.method(11).invoke(this._obj, [
      _unwrap(context),
      Array.isArray(promptEmbedding)
        ? DynWinRtValue.createVector(
            promptEmbedding.map((_i) => _unwrap(_i)),
            DynWinRtType.runtimeClass(
              'Microsoft.Windows.AI.Foundation.EmbeddingVector',
              WinGuid.parse('07bdaa90-b3d2-5701-97d1-c390ec62799c'),
            ),
          )
        : _unwrap(promptEmbedding),
      _unwrap(options),
    ]);
    const _onAbort = signal
      ? () => {
          try {
            _op.cancel();
          } catch (_ce) {
            /* cancel after completion is a no-op per WinRT spec */
          }
        }
      : undefined;
    if (_onAbort) {
      signal.addEventListener('abort', _onAbort, { once: true });
      if (signal.aborted) _onAbort();
    }
    const _wrap = (p) =>
      p
        .then(
          (_v) => new LanguageModelResponseResult(_v),
          (_e) => {
            if (signal?.aborted) throw signal.reason;
            throw _e;
          },
        )
        .finally(() => {
          if (_onAbort) signal.removeEventListener('abort', _onAbort);
        });
    const _promise = _wrap(_op.toPromise());
    return Object.assign(_promise, {
      progress(cb) {
        _op.onProgress((_p) => cb(_p.toString()));
        return this;
      },
      toPromise() {
        return _wrap(_op.toPromise());
      },
      cancel() {
        try {
          _op.cancel();
        } catch (_ce) {
          /* cancel after completion is a no-op per WinRT spec */
        }
      },
    });
  }

  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @returns The embedding vector representing the string prompt.
   */
  generateEmbeddingVectors(prompt) {
    return new LanguageModelEmbeddingVectorResult(
      _ILanguageModel2
        .method(12)
        .invoke(this._obj, [DynWinRtValue.hstring(prompt)]),
    );
  }

  /**
   * Returns an embedding vector representing the string prompt.
   * @param prompt The string prompt.
   * @param contentFilterOptions Options that affect how the language model filters responses to the prompt.
   */
  generateEmbeddingVectors2(prompt, contentFilterOptions) {
    return new LanguageModelEmbeddingVectorResult(
      _ILanguageModel2
        .method(13)
        .invoke(this._obj, [
          DynWinRtValue.hstring(prompt),
          _unwrap(contentFilterOptions),
        ]),
    );
  }

  /**
   * Returns the index in the given prompt where the context window reached the maximum number of supported tokens.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength(prompt) {
    return _ILanguageModel2
      .method(14)
      .invoke(this._obj, [DynWinRtValue.hstring(prompt)])
      .toI64();
  }

  /**
   * Returns the index in the given prompt where the given context window reached the maximum number of supported tokens.
   * @param context The context window.
   * @param prompt The input prompt.
   * @returns The index in the given prompt where the given context window reached the maximum number of supported tokens.
   */
  getUsablePromptLength2(context, prompt) {
    return _ILanguageModel2
      .method(15)
      .invoke(this._obj, [_unwrap(context), DynWinRtValue.hstring(prompt)])
      .toI64();
  }

  /**
   * Returns the unique identifier of an embedding vector object.
   * @returns The unique identifier of an embedding vector object.
   */
  getVectorSpaceId() {
    return _ILanguageModel2.method(16).invoke(this._obj, []).toString();
  }

  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @returns A context window
   */
  createContext() {
    return new LanguageModelContext(
      _ILanguageModel2.method(17).invoke(this._obj, []),
    );
  }

  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @param systemPrompt The string prompt.
   * @returns A context window
   */
  createContext2(systemPrompt) {
    return new LanguageModelContext(
      _ILanguageModel2
        .method(18)
        .invoke(this._obj, [DynWinRtValue.hstring(systemPrompt)]),
    );
  }

  /**
   * Returns a context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response.
   * @param systemPrompt The string prompt.
   * @param contentFilterOptions Options that affect how the language model filters responses to the prompt.
   * @returns A context window
   */
  createContext3(systemPrompt, contentFilterOptions) {
    return new LanguageModelContext(
      _ILanguageModel2
        .method(19)
        .invoke(this._obj, [
          DynWinRtValue.hstring(systemPrompt),
          _unwrap(contentFilterOptions),
        ]),
    );
  }
}
